let numbers = [5, 6, 7, 8]
let letters = ['a', 'b', 'c']

//Comando para concatenar
all = numbers.concat(letters)

//Concatena a partir do index que for indicado
half = all.slice(3, 5)

console.log(all)
console.log(half)